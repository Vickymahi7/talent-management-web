export enum UserType {
  SAD = "Super Admin",
  ADM = "Admin",
  HRU = "Hr User",
  USR = "User",
}

export enum UserTypeId {
  SAD = 1,
  ADM = 2,
  HRU = 3,
  USR = 4,
}

export enum AccountStatus {
  ACT = "Active",
  BLO = "Blocked",
  SUS = "Suspended",
}

export enum AccountStatusId {
  ACT = 1,
  BLO = 2,
  SUS = 3,
}
